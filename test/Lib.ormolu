module Lib where

-- We want both ormolu and fourmolu to format this, and the latter in such
-- a way that it takes fourmolu.yaml into account
hello :: IO ()
hello = do
  putStrLn "hello"
  putStrLn greeting
  where
    greeting = "there"
